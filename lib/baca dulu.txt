ini kode sql nya

CREATE DATABASE db_nextfly;
use db_nextfly;

CREATE TABLE user (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    nama_lengkap VARCHAR(100) NOT NULL
);

INSERT INTO user (username, password, nama_lengkap) VALUES
('Yanto', '123456', 'Yanto Tarmiji');

CREATE TABLE tiket (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nama_tiket VARCHAR(100) NOT NULL,
    harga DECIMAL(12,2) NOT NULL,
    stok_tiket INT NOT NULL
);

INSERT INTO tiket (nama_tiket, harga, stok_tiket) VALUES
('Konser Jazz', 250000, 100),
('Festival Kuliner', 75000, 200);

CREATE TABLE transaksi (
    id INT AUTO_INCREMENT PRIMARY KEY,
    tiket_id INT NOT NULL,
    jumlah INT NOT NULL,
    total_harga DECIMAL(14,2) NOT NULL,
    tanggal DATETIME DEFAULT CURRENT_TIMESTAMP
);
